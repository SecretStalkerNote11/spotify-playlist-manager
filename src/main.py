[
    {
        "path": "spotify_playlist_manager/auth.py",
        "content": "import os\nimport webbrowser\nfrom typing import Optional\nimport requests\nfrom urllib.parse import urlencode, urlparse, parse_qs\n\nSPOTIFY_AUTH_URL = \"https://accounts.spotify.com/authorize\"\nSPOTIFY_TOKEN_URL = \"https://accounts.spotify.com/api/token\"\nSPOTIFY_API_BASE = \"https://api.spotify.com/v1\"\n\nSCOPES = [\n    \"playlist-read-private\",\n    \"playlist-read-collaborative\",\n    \"playlist-modify-public\",\n    \"playlist-modify-private\",\n]\n\n\nclass SpotifyAuth:\n    \"\"\"Handles OAuth 2.0 authentication with Spotify.\"\"\"\n\n    def __init__(self, client_id: str, client_secret: str, redirect_uri: str = \"http://localhost:8888/callback\"):\n        self.client_id = client_id\n        self.client_secret = client_secret\n        self.redirect_uri = redirect_uri\n        self.access_token: Optional[str] = None\n        self.refresh_token: Optional[str] = None\n\n    def get_auth_url(self) -> str:\n        \"\"\"Generate the authorization URL for user consent.\"\"\"\n        params = {\n            \"client_id\": self.client_id,\n            \"response_type\": \"code\",\n            \"redirect_uri\": self.redirect_uri,\n            \"scope\": \" \".join(SCOPES),\n            \"show_dialog\": True,\n        }\n        return f\"{SPOTIFY_AUTH_URL}?{urlencode(params)}\"\n\n    def request_tokens(self, auth_code: str) -> None:\n        \"\"\"Exchange authorization code for access and refresh tokens.\"\"\"\n        payload = {\n            \"grant_type\": \"authorization_code\",\n            \"code\": auth_code,\n            \"redirect_uri\": self.redirect_uri,\n            \"client_id\": self.client_id,\n            \"client_secret\": self.client_secret,\n        }\n        headers = {\"Content-Type\": \"application/x-www-form-urlencoded\"}\n        response = requests.post(SPOTIFY_TOKEN_URL, data=payload, headers=headers)\n        response.raise_for_status()\n        data = response.json()\n        self.access_token = data[\"access_token\"]\n        self.refresh_token = data.get(\"refresh_token\")\n\n    def refresh_access_token(self) -> None:\n        \"\"\"Refresh the access token using the refresh token.\"\"\"\n        if not self.refresh_token:\n            raise ValueError(\"No refresh token available.\")\n        payload = {\n            \"grant_type\": \"refresh_token\",\n            \"refresh_token\": self.refresh_token,\n            \"client_id\": self.client_id,\n            \"client_secret\": self.client_secret,\n        }\n        headers = {\"Content-Type\": \"application/x-www-form-urlencoded\"}\n        response = requests.post(SPOTIFY_TOKEN_URL, data=payload, headers=headers)\n        response.raise_for_status()\n        data = response.json()\n        self.access_token = data[\"access_token\"]\n        if \"refresh_token\" in data:\n            self.refresh_token = data[\"refresh_token\"]\n\n    def get_headers(self) -> dict:\n        \"\"\"Return authorization headers for API requests.\"\"\"\n        if not self.access_token:\n            raise RuntimeError(\"No access token. Authenticate first.\")\n        return {\"Authorization\": f\"Bearer {self.access_token}\"}\n\n    def authenticate(self) -> None:\n        \"\"\"Run the full OAuth flow via browser and local server.\n        Note: This is a simplified version; in production use a proper HTTP server.\n        \"\"\"\n        auth_url = self.get_auth_url()\n        print(\"Opening browser for Spotify authorization...\")\n        webbrowser.open(auth_url)\n        # In a real implementation, you'd start a local server to capture the callback.\n        # For demonstration, we prompt the user to paste the callback URL.\n        callback_url = input(\"Paste the full redirect URL here: \").strip()\n        parsed = urlparse(callback_url)\n        code = parse_qs(parsed.query).get(\"code\", [None])[0]\n        if not code:\n            raise ValueError(\"No authorization code found in callback.\")\n        self.request_tokens(code)\n        print(\"Authentication successful.\")\n"
    },
    {
        "path": "spotify_playlist_manager/transfer.py",
        "content": "from typing import List, Dict, Optional\nimport requests\nfrom .auth import SpotifyAuth, SPOTIFY_API_BASE\n\n\nclass PlaylistTransfer:\n    \"\"\"Handles transferring playlists between Spotify accounts.\"\"\"\n\n    def __init__(self, auth: SpotifyAuth):\n        self.auth = auth\n\n    def get_user_playlists(self, user_id: str) -> List[Dict]:\n        \"\"\"Fetch all playlists for a given user.\"\"\"\n        playlists = []\n        url = f\"{SPOTIFY_API_BASE}/users/{user_id}/playlists\"\n        while url:\n            response = requests.get(url, headers=self.auth.get_headers())\n            response.raise_for_status()\n            data = response.json()\n            playlists.extend(data[\"items\"])\n            url = data.get(\"next\")\n        return playlists\n\n    def get_playlist_tracks(self, playlist_id: str) -> List[Dict]:\n        \"\"\"Retrieve all tracks from a playlist.\"\"\"\n        tracks = []\n        url = f\"{SPOTIFY_API_BASE}/playlists/{playlist_id}/tracks\"\n        while url:\n            response = requests.get(url, headers=self.auth.get_headers())\n            response.raise_for_status()\n            data = response.json()\n            tracks.extend(data[\"items\"])\n            url = data.get(\"next\")\n        return tracks\n\n    def create_playlist(self, user_id: str, name: str, description: str = \"\", public: bool = True) -> Optional[str]:\n        \"\"\"Create a new playlist and return its ID.\"\"\"\n        url = f\"{SPOTIFY_API_BASE}/users/{user_id}/playlists\"\n        payload = {\n            \"name\": name,\n            \"description\": description,\n            \"public\": public,\n        }\n        response = requests.post(url, json=payload, headers=self.auth.get_headers())\n        if response.status_code == 201:\n            return response.json()[\"id\"]\n        else:\n            print(f\"Failed to create playlist: {response.status_code} - {response.text}\")\n            return None\n\n    def add_tracks_to_playlist(self, playlist_id: str, track_uris: List[str]) -> bool:\n        \"\"\"Add tracks to a playlist in batches of 100.\"\"\"\n        url = f\"{SPOTIFY_API_BASE}/playlists/{playlist_id}/tracks\"\n        for i in range(0, len(track_uris), 100):\n            batch = track_uris[i:i+100]\n            payload = {\"uris\": batch}\n            response = requests.post(url, json=payload, headers=self.auth.get_headers())\n            if response.status_code not in (200, 201):\n                print(f\"Failed to add tracks batch: {response.status_code}\")\n                return False\n        return True\n\n    def transfer_playlist(self, source_user_id: str, target_user_id: str, playlist_id: str,\n                         new_name: Optional[str] = None, new_description: Optional[str] = None) -> bool:\n        \"\"\"Transfer a playlist from source to target user.\"\"\"\n        # Get playlist details\n        url = f\"{SPOTIFY_API_BASE}/playlists/{playlist_id}\"\n        response = requests.get(url, headers=self.auth.get_headers())\n        response.raise_for_status()\n        playlist_data = response.json()\n\n        name = new_name or playlist_data[\"name\"]\n        description = new_description or playlist_data.get(\"description\", \"\")\n\n        # Get all tracks\n        tracks = self.get_playlist_tracks(playlist_id)\n        track_uris = [item[\"track\"][\"uri\"] for item in tracks if item.get(\"track\")]\n\n        if not track_uris:\n            print(\"No tracks to transfer.\")\n            return False\n\n        # Create new playlist\n        new_playlist_id = self.create_playlist(target_user_id, name, description)\n        if not new_playlist_id:\n            return False\n\n        # Add tracks\n        success = self.add_tracks_to_playlist(new_playlist_id, track_uris)\n        if success:\n            print(f\"Successfully transferred playlist '{name}' with {len(track_uris)} tracks.\")\n        else:\n            print(f\"Partial transfer may have occurred for '{name}'.\")\n        return success\n"
    },
    {
        "path": "spotify_playlist_manager/cli.py",
        "content": "import argparse\nimport os\nimport sys\nfrom dotenv import load_dotenv\nfrom .auth import SpotifyAuth\nfrom .transfer import PlaylistTransfer\n\n\ndef main():\n    \"\"\"Command-line interface for Spotify Playlist Manager.\"\"\"\n    load_dotenv()\n\n    parser = argparse.ArgumentParser(\n        description=\"Transfer playlists between Spotify accounts.\"\n    )\n    parser.add_argument(\n        \"--source\",\n        required=True,\n        help=\"Source Spotify user ID\"\n    )\n    parser.add_argument(\n        \"--target\",\n        required=True,\n        help=\"Target Spotify user ID\"\n    )\n    parser.add_argument(\n        \"--playlist-id\",\n        required=True,\n        help=\"Playlist ID to transfer\"\n    )\n    parser.add_argument(\n        \"--new-name\",\n        default=None,\n        help=\"Optional new name for the playlist\"\n    )\n    parser.add_argument(\n        \"--new-description\",\n        default=None,\n        help=\"Optional new description for the playlist\"\n    )\n\n    args = parser.parse_args()\n\n    client_id = os.getenv(\"SPOTIFY_CLIENT_ID\")\n    client_secret = os.getenv(\"SPOTIFY_CLIENT_SECRET\")\n\n    if not client_id or not client_secret:\n        print(\"Error: SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET must be set in .env file or environment.\")\n        sys.exit(1)\n\n    auth = SpotifyAuth(client_id, client_secret)\n    try:\n        auth.authenticate()\n    except Exception as e:\n        print(f\"Authentication failed: {e}\")\n        sys.exit(1)\n\n    transfer = PlaylistTransfer(auth)\n    success = transfer.transfer_playlist(\n        source_user_id=args.source,\n        target_user_id=args.target,\n        playlist_id=args.playlist_id,\n        new_name=args.new_name,\n        new_description=args.new_description,\n    )\n\n    if success:\n        print(\"Transfer completed successfully.\")\n    else:\n        print(\"Transfer failed.\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n"
    },
    {
        "path": "spotify_playlist_manager/__init__.py",
        "content": "\"\"\"Spotify Playlist Manager - A tool for transferring playlists between Spotify accounts.\"\"\"\n\n__version__ = \"1.0.0\"\n"
    },
    {
        "path": "requirements.txt",
        "content": "requests==2.31.0\npython-dotenv==1.0.0\n"
    },
    {
        "path": "tests/test_transfer.py",
        "content": "import unittest\nfrom unittest.mock import patch, MagicMock\nfrom spotify_playlist_manager.auth import SpotifyAuth\nfrom spotify_playlist_manager.transfer import PlaylistTransfer\n\n\nclass TestPlaylistTransfer(unittest.TestCase):\n    \"\"\"Unit tests for PlaylistTransfer class.\"\"\"\n\n    def setUp(self):\n        self.auth = SpotifyAuth(\"test_client_id\", \"test_client_secret\")\n        self.auth.access_token = \"fake_token\"\n        self.transfer = PlaylistTransfer(self.auth)\n\n    @patch(\"spotify_playlist_manager.transfer.requests.get\")\n    def test_get_user_playlists(self, mock_get):\n        \"\"\"Test fetching user playlists returns list.\"\"\"\n        mock_response = MagicMock()\n        mock_response.json.return_value = {\n            \"items\": [{\"id\": \"1\", \"name\": \"Test Playlist\"}],\n            \"next\": None\n        }\n        mock_response.raise_for_status.return_value = None\n        mock_get.return_value = mock_response\n\n        playlists = self.transfer.get_user_playlists(\"test_user\")\n        self.assertEqual(len(playlists), 1)\n        self.assertEqual(playlists[0][\"name\"], \"Test Playlist\")\n\n    @patch(\"spotify_playlist_manager.transfer.requests.get\")\n    def test_get_playlist_tracks(self, mock_get):\n        \"\"\"Test retrieving tracks from a playlist.\"\"\"\n        mock_response = MagicMock()\n        mock_response.json.return_value = {\n            \"items\": [\n                {\"track\": {\"uri\": \"spotify:track:abc123\", \"name\": \"Song A\"}},\n                {\"track\": {\"uri\": \"spotify:track:def456\", \"name\": \"Song B\"}},\n            ],\n            \"next\": None\n        }\n        mock_response.raise_for_status.return_value = None\n        mock_get.return_value = mock_response\n\n        tracks = self.transfer.get_playlist_tracks(\"playlist_123\")\n        self.assertEqual(len(tracks), 2)\n        self.assertEqual(tracks[0][\"track\"][\"uri\"], \"spotify:track:abc123\")\n\n    @patch(\"spotify_playlist_manager.transfer.requests.post\")\n    def test_create_playlist_success(self, mock_post):\n        \"\"\"Test successful playlist creation.\"\"\"\n        mock_response = MagicMock()\n        mock_response.status_code = 201\n        mock_response.json.return_value = {\"id\": \"new_playlist_456\"}\n        mock_post.return_value = mock_response\n\n        playlist_id = self.transfer.create_playlist(\"user1\", \"My New Playlist\")\n        self.assertEqual(playlist_id, \"new_playlist_456\")\n\n    @patch(\"spotify_playlist_manager.transfer.requests.post\")\n    def test_create_playlist_failure(self, mock_post):\n        \"\"\"Test playlist creation failure.\"\"\"\n        mock_response = MagicMock()\n        mock_response.status_code = 400\n        mock_response.text = \"Bad Request\"\n        mock_post.return_value = mock_response\n\n        playlist_id = self.transfer.create_playlist(\"user1\", \"Failing Playlist\")\n        self.assertIsNone(playlist_id)\n\n    @patch(\"spotify_playlist_manager.transfer.requests.post\")\n    def test_add_tracks_to_playlist(self, mock_post):\n        \"\"\"Test adding tracks to a playlist.\"\"\"\n        mock_response = MagicMock()\n        mock_response.status_code = 201\n        mock_post.return_value = mock_response\n\n        uris = [\"spotify:track:1\", \"spotify:track:2\"]\n        result = self.transfer.add_tracks_to_playlist(\"playlist_789\", uris)\n        self.assertTrue(result)\n\n    @patch(\"spotify_playlist_manager.transfer.PlaylistTransfer.get_playlist_tracks\")\n    @patch(\"spotify_playlist_manager.transfer.PlaylistTransfer.create_playlist\")\n    @patch(\"spotify_playlist_manager.transfer.PlaylistTransfer.add_tracks_to_playlist\")\n    @patch(\"spotify_playlist_manager.transfer.requests.get\")\n    def test_transfer_playlist_success(self, mock_get, mock_add, mock_create, mock_get_tracks):\n        \"\"\"Test full playlist transfer flow.\"\"\"\n        # Mock playlist details\n        mock_get_response = MagicMock()\n        mock_get_response.json.return_value = {\n            \"name\": \"Original Playlist\",\n            \"description\": \"Test desc\"\n        }\n        mock_get_response.raise_for_status.return_value = None\n        mock_get.return_value = mock_get_response\n\n        # Mock tracks\n        mock_get_tracks.return_value = [\n            {\"track\": {\"uri\": \"spotify:track:abc\"}},\n            {\"track\": {\"uri\": \"spotify:track:def\"}},\n        ]\n\n        # Mock playlist creation\n        mock_create.return_value = \"new_playlist_id\"\n\n        # Mock adding tracks\n        mock_add.return_value
